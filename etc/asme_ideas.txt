* positional qgrams: exactly before removing false positives {qgram: [1, 2 #positions]} (check screenshot)
* store the evaluation results in a collection
* john -> !j, jo, oh, hn, n%
* capability of querying strings with length <= QGRAM_LENGTH+1
* use tries to cache previously calculated edit distance
